<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 实心 -->
    <solid android:color="@color/returnhome" />
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/returnhome" />
    <!-- 圆角 -->
    <corners android:radius="6dp" />

    <padding
        android:bottom="5dp"
        android:left="25dp"
        android:right="25dp"
        android:top="5dp" />

</shape>